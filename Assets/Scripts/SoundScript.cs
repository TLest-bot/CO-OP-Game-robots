using UnityEngine;

public class SoundScript : MonoBehaviour
{
    public AudioSource myAudio;
    void Start()
    {
        myAudio.Play();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
